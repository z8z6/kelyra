@Vertex
struct Pair {
  first: i32,
  second: i32,
}

fn sum(values: i32[4], count: i32) -> i32 {
  let total: i32 = 0;
  let i = 0;
  let s = "hello world!";
  while i < count {
    total = total + values[i];
    i = i + 1;
  }
  if total > 0 {
    return total;
  } else {
    return 0;
  }
}

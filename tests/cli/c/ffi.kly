import c "print.h";

pub fn main() -> i32 {
  let pair: c.Pair = c.make_pair(20, 22);
  let pointer: c.Pair* = c.pair_pointer();
  let total = c.sum_many(2, c.sum_pair(pair), c.sum_pair_pointer(pointer));
  c.print_format("total=%d\n", total);
  return 0;
}
